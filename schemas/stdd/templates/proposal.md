## Why

<!-- 说明本次变更的动机：要解决什么问题？为什么是现在？ -->

## What Changes

<!-- 描述将要发生的变更。明确列出新增能力、修改点或移除项，尽可能使用中文描述 -->

## Capabilities

### New Capabilities
<!-- 新引入的能力。将 <name> 替换为 kebab-case 标识符（例如 user-auth、data-export、api-rate-limiting），每一项会生成 specs/<name>/spec.md -->
- `<name>`: <该能力所覆盖范围的简要描述>

### Modified Capabilities
<!-- REQUIREMENTS 发生变化的既有能力（不仅是实现细节调整）。
     仅当 spec 级别行为变化时才列入。每项需要一个 delta spec 文件。
     使用 openspec/specs/ 下既有的 spec 名称。若无 requirement 变化则留空。 -->
- `<existing-name>`: <变化的 requirement 内容>


## Goals
<!-- 任何变更必须附带测试用例清单，列出输入、预期输出和边界条件。实现代码在未通过测试之前视为未完成 -->

## Impact

<!-- 受影响的代码、API、依赖、系统 -->
